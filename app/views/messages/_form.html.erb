<% if user_signed_in? %>
    <%= simple_form_for @message do |f| %> <%= f.input :title, label: "Title" %> <%=
    f.input :desc, label: "Description" %> <%= f.button :submit, class: "btn
    btn-default" %> <% end %>
<% end %>